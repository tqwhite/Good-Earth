<%var appearance=appearance;var divId=controllerHelper.getDivId(divPrefix);if(appearance.title){ //note it is often convenient to generate the tooltip at the calling routine instead of using this	var selector = '#'+divId;	cleanUpObject.addCleanupFunction(		function(values) { 			controllerHelper.addStandardToolTip(values.selector, { delay: 2 }) 		},		{selector:selector}	);	var showTitle="title='"+appearance.title+"'";		}	else{showTitle='';}	%><span id='<%==divId%>' <%==showTitle%>><%==appearance.label%></span>